feat: add comment reporting UI and sensitive words admin management

Admin:
- Add admin-comment-reports component: list reports by status, review/dismiss
- Add admin-sensitive-words component: CRUD for comment filtering
- Register both routes in app.routes.ts
- Add menu items in admin-shell (举报管理, 敏感词)

Frontend:
- Add report button to comment items (yellow, non-own comments only)
- reportComment() method: POST to /comments/:id/report with reason
- Inject HttpClient into LiveCommentComponent

Co-Authored-By: AtomCode (deepseek-v4-flash) <noreply@atomgit.com>
