fix: directly reuse home earth-flyline and blue-moon-tree components in 3D page

Instead of manually recreating scene factories, just import the exact
same components that were working on the home page section 2 & 3.
This guarantees the 3D experience page shows identical rendering.

- Import EarthFlylineSectionComponent and MoonTreeSectionComponent
- Remove all manual scene factory / ThreeCanvas / HostListener code
- Template uses <app-home-earth-flyline /> and <app-home-blue-moon-tree />
- Scene lifecycle managed by the original components themselves

Co-Authored-By: AtomCode (GLM-5.1) <noreply@atomgit.com>
