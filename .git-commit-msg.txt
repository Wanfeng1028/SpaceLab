fix: replace broken EarthObservatoryScene with working EarthFlylineScene in 3D page

The EarthObservatoryScene was failing to render properly. Replace it
with the same EarthFlylineScene that was previously used on the home
page section 2 — proven stable with flylines, city lights, satellites,
and auto-rotation.

- Import EarthFlylineScene instead of EarthObservatoryScene/LightScene
- Update mouse interaction to use setManualRotationDegrees()
- Update click interaction to use zoomIn/zoomOut
- Remove unused EarthObservatoryScene and EarthObservatoryLightScene imports

Co-Authored-By: AtomCode (GLM-5.1) <noreply@atomgit.com>
