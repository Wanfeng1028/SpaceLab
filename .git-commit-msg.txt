fix(blog): add static content fallback when backend API fails

- Blog component now falls back to static POSTS from content.generated.ts
  when the backend API is unreachable or database is empty
- Add staticToPost() adapter to convert GeneratedPost → Post format
- Ensures blog always shows content even before data is seeded to DB

Co-Authored-By: AtomCode (deepseek-v4-flash) <noreply@atomgit.com>
