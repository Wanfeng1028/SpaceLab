chore: code review fixes — fix goroutine leak, ILIKE compat, add tests

- Fix scheduled publish goroutine: use context cancellation (schedCtx/schedCancel)
  to properly stop ticker on server shutdown
- Fix AiToolService.List: replace PostgreSQL-only ILIKE with LOWER()
  for cross-DB compatibility (PostgreSQL + SQLite)
- Fix NG8107 warning: replace e.user_id?.slice with e.user_id.slice
- Add AiNewsService + AiToolService unit tests

Co-Authored-By: AtomCode (deepseek-v4-flash) <noreply@atomgit.com>
